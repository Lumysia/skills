---
name: sys-install
description: Install operating-system software. Use when the user asks to install software with source selection, version comparison, package-manager handling, and operation logging.
---

# Sys Install

Run the repository skill `sys-software-lifecycle` for an install request.

User request: $ARGUMENTS

Read `sys-software-lifecycle/SKILL.md` from the registered skills repository and follow its referenced files. This wrapper only exposes `sys-install` globally for Codex hosts that scan `~/.codex/skills`.
