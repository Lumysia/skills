---
description: Initialize agent host config, skill paths, and requested MCP servers.
---

Run the repository skill `eng-agent-init`.

Read `eng-agent-init/SKILL.md` and follow its referenced files. This file only exposes `/setup` for Codex versions that load project commands.
